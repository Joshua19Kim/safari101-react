'use strict';

/**
 * photographic-safari controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::photographic-safari.photographic-safari');
